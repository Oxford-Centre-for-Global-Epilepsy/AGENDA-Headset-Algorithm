head:
  type: hierarchical
  hidden_dim: 0
  num_classes: [1,1,1]
  l2_weight: 1e-3

loss:
  _target_: ml_tflm.training.loss.HierarchicalLoss

evaluator:
  _target_: ml_tflm.training.metrics.metric_evaluator
  label_config: null
  prediction_caster: cast_prediction_hierarchical

model_target_lookup:
  targets: labels
  label_mask: label_mask
  entropy_targets: internal_label